<Window x:Class="WebAppInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Web Application Installer"
        Height="626"
        Width="948"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Application Name"
            FontWeight="Bold"
            Margin="0,0,0,5"/>

        <TextBox
            x:Name="txtAppName"
            Grid.Row="1"
            Height="30"/>

        <Button
            x:Name="btnDeploy"
            Grid.Row="2"
            Margin="0,20,0,20"
            Height="35"
            Width="120"
            HorizontalAlignment="Center"
            Content="Deploy"
            Click="Deploy_Click"/>

        <TextBlock
            x:Name="txtStatus"
            Grid.Row="3"
            Text="Ready..."
            FontSize="14"
            Foreground="Blue"/>

    </Grid>
</Window>